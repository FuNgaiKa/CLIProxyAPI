# CLIProxyAPI 快速上手指南

## 简介

CLIProxyAPI 是一个多 AI 提供商统一代理服务器，可以让你使用多个 Claude API Key 进行负载均衡，避免单个 Key 配额用尽。

## 🚀 开始使用

### **步骤 0：配置文件准备**

在启动服务前，确保 `config.yaml` 配置正确：

```yaml
# API keys for authentication (留空表示不需要认证即可访问代理服务)
api-keys: []

# Claude API keys (这里配置你的 Claude API Key)
claude-api-key:
  - api-key: "你的第一个key"
    base-url: "https://www.88code.org/api"
```

**重要说明**：
- `api-keys`: 用于访问代理服务器的认证，设置为 `[]` 表示不需要认证
- `claude-api-key`: 用于调用 Claude API 的密钥，这是负载均衡的关键配置

---

### **步骤 1：编译并启动服务**

在项目目录下运行：

```bash
cd C:\Users\russell.fu\GolandProjects\CLIProxyAPI

# 编译
go build -o cli-proxy-api.exe ./cmd/server

# 启动服务
.\cli-proxy-api.exe
```

看到类似 `Server started on :8317` 就成功了。

---

### **步骤 2：测试是否正常**

打开新的 PowerShell 窗口，测试：

```powershell
# PowerShell 推荐使用
Invoke-WebRequest -Uri http://localhost:8317/v1/models | Select-Object -ExpandProperty Content

# 或者使用 curl.exe（如果安装了 Git for Windows）
curl.exe http://localhost:8317/v1/models
```

应该会返回可用的模型列表。

---

### **步骤 3：使用 Claude**

#### 方法 A：使用 PowerShell 测试

```powershell
# PowerShell 测试
$body = @{
    model = "claude-sonnet-4-20250514"
    messages = @(
        @{
            role = "user"
            content = "你好"
        }
    )
    max_tokens = 1000
} | ConvertTo-Json

Invoke-WebRequest -Uri http://localhost:8317/v1/messages `
  -Method POST `
  -ContentType "application/json" `
  -Body $body | Select-Object -ExpandProperty Content
```

或使用 curl.exe：

```bash
curl.exe http://localhost:8317/v1/messages ^
  -H "Content-Type: application/json" ^
  -d "{\"model\":\"claude-sonnet-4-20250514\",\"messages\":[{\"role\":\"user\",\"content\":\"你好\"}],\"max_tokens\":1000}"
```

#### 方法 B：配置 Claude Code 使用代理

**重要提示**：环境变量设置后，必须**在同一个窗口**启动 Claude Code 才能生效！

##### 🔹 方式 1：使用 PowerShell（推荐）

打开 PowerShell，执行：

```powershell
$env:ANTHROPIC_BASE_URL="http://127.0.0.1:8317"
$env:ANTHROPIC_AUTH_TOKEN="sk-dummy"
$env:ANTHROPIC_MODEL="claude-sonnet-4-20250514"
$env:ANTHROPIC_SMALL_FAST_MODEL="claude-3-5-haiku-20241022"

# 在同一窗口启动 Claude Code
claude
```

##### 🔹 方式 2：使用 CMD

打开 CMD，执行：

```cmd
set ANTHROPIC_BASE_URL=http://127.0.0.1:8317
set ANTHROPIC_AUTH_TOKEN=sk-dummy
set ANTHROPIC_MODEL=claude-sonnet-4-20250514
set ANTHROPIC_SMALL_FAST_MODEL=claude-3-5-haiku-20241022

claude
```

**注意**：
- ⚠️ PowerShell 和 CMD 的语法**不同**，不能混用
- ⚠️ 必须在**设置环境变量的同一窗口**启动 Claude
- ⚠️ 这种方式只在当前窗口有效，关闭后失效

##### 🔹 方式 3：系统环境变量（永久生效，推荐日常使用）

如果想每次启动 Claude Code 都自动使用代理：

1. 按 `Win + R`，输入 `sysdm.cpl`，回车
2. 点击「高级」→「环境变量」
3. 在「用户变量」中点击「新建」，依次添加以下变量：

| 变量名 | 变量值 |
|--------|--------|
| `ANTHROPIC_BASE_URL` | `http://127.0.0.1:8317` |
| `ANTHROPIC_AUTH_TOKEN` | `sk-dummy` |
| `ANTHROPIC_MODEL` | `claude-sonnet-4-20250514` |
| `ANTHROPIC_SMALL_FAST_MODEL` | `claude-3-5-haiku-20241022` |

4. 点击「确定」保存
5. **重启 Claude Code**（已打开的需要完全关闭后重新打开）

##### ✅ 验证是否配置成功

启动 Claude Code 后，输入 `/status`，检查输出：

```
Anthropic base URL: http://127.0.0.1:8317  ✅ 正确
```

如果显示的是其他地址（如 `https://www.88code.org/api`），说明环境变量没有生效，请检查：
1. 是否在同一窗口启动（临时方式）
2. 是否重启了 Claude Code（系统环境变量方式）

---

## 📝 添加更多 Claude Key 实现负载均衡

如果你有多个 Claude API Key，编辑 `config.yaml`：

```yaml
claude-api-key:
  - api-key: "第一个key"
    base-url: "https://www.88code.org/api"
  - api-key: "第二个key"
    base-url: "https://www.88code.org/api"
  - api-key: "第三个key"
    base-url: "https://www.88code.org/api"
```

保存后，服务会**自动热重载**配置，无需重启！

服务会**自动轮流使用**这些 Key，实现负载均衡：
- 每次请求依序轮流使用不同的 API Key
- 配额超出时自动切换到下一个 Key
- 某个 Key 不可用时自动跳过

---

## 🎯 支持的 Claude 模型

- claude-opus-4-1-20250805
- claude-opus-4-20250514
- claude-sonnet-4-20250514
- claude-sonnet-4-5-20250929
- claude-3-7-sonnet-20250219
- claude-3-5-haiku-20241022

---

## 🔧 配置代理（可选）

如果你需要使用代理访问 Claude API，可以在 `config.yaml` 中配置：

### 全局代理
```yaml
proxy-url: "socks5://127.0.0.1:1080"
```

### 为每个 Key 单独配置代理
```yaml
claude-api-key:
  - api-key: "key1"
    base-url: "https://www.88code.org/api"
    proxy-url: "socks5://proxy1.example.com:1080"
  - api-key: "key2"
    base-url: "https://www.88code.org/api"
    proxy-url: "socks5://proxy2.example.com:1080"
```

---

## 📊 查看日志

日志文件位置：
- Windows: `C:\Users\russell.fu\GolandProjects\CLIProxyAPI\logs\`

如果你想在控制台直接查看日志，修改 `config.yaml`：
```yaml
logging-to-file: false
```

---

## ❓ 常见问题

### Q: 访问时提示 "Missing API key" 错误？
A: 这是因为 `api-keys` 配置了示例值。修改 `config.yaml`：
```yaml
api-keys: []  # 设置为空数组，表示不需要认证
```
然后重启服务。

**注意**：`api-keys` 和 `claude-api-key` 是两个不同的配置：
- `api-keys`: 访问代理服务器的认证（留空表示公开访问）
- `claude-api-key`: 调用 Claude API 的密钥（必须配置）

### Q: 服务启动后无法访问？
A: 检查防火墙是否允许 8317 端口，或者在 `config.yaml` 中修改端口。

### Q: 如何知道当前使用的是哪个 Key？
A: 启用 debug 模式：
```yaml
debug: true
```

### Q: 支持官方 Claude API Key 吗？
A: 支持！配置示例：
```yaml
claude-api-key:
  - api-key: "sk-ant-api03-..."  # 官方 key，不需要设置 base-url
```

### Q: PowerShell 中 curl 命令报错？
A: PowerShell 中的 `curl` 是 `Invoke-WebRequest` 的别名，行为不同。建议：
- 使用 `Invoke-WebRequest` 命令
- 或使用 `curl.exe` 来调用真正的 curl 程序

### Q: 服务启动后提示"��法连接到远程服务器"？
A: 检查服务是否真的启动成功：
1. 查看启动日志，应该有 `API server started successfully`
2. 执行 `tasklist | findstr cli-proxy` 检查进程是否存在
3. 如果服务正常，测试：`curl http://localhost:8317/v1/models`

### Q: Claude Code 的 `/status` 显示的 base URL 不是代理地址？
A: 说明环境变量没有生效。解决方法：

**临时方式**：
- 必须在设置环境变量的**同一个窗口**启动 Claude
- 验证环境变量：
  - CMD: `echo %ANTHROPIC_BASE_URL%`
  - PowerShell: `echo $env:ANTHROPIC_BASE_URL`

**永久方式**：
- 设置系统环境变量后，必须**完全关闭并重新打开** Claude Code
- 可能需要重启电脑才能生效

**正确的 status 输出应该是**：
```
Anthropic base URL: http://127.0.0.1:8317  ✅
```

### Q: 在 CMD 中执行 PowerShell 命令报错？
A: PowerShell 和 CMD 是不同的终端，语法不同：
- **PowerShell 语法**：`$env:变量名="值"`
- **CMD 语法**：`set 变量名=值`（注意没有空格、没有引号）

确认你的终端类型：
- CMD 提示符：`C:\Users\xxx>`
- PowerShell 提示符：`PS C:\Users\xxx>`

---

## 🌟 高级功能

### 使用 Python 调用

```python
import requests

response = requests.post(
    "http://localhost:8317/v1/messages",
    json={
        "model": "claude-sonnet-4-20250514",
        "messages": [{"role": "user", "content": "你好"}],
        "max_tokens": 1000
    }
)

print(response.json())
```

### 使用 OpenAI SDK 调用

```python
from openai import OpenAI

client = OpenAI(
    api_key="dummy",  # 不使用但必需
    base_url="http://localhost:8317/v1"
)

response = client.chat.completions.create(
    model="claude-sonnet-4-20250514",
    messages=[{"role": "user", "content": "你好"}]
)

print(response.choices[0].message.content)
```

---

## 📖 更多功能

- 支持 Gemini、OpenAI、Qwen、iFlow 等多个提供商
- 管理 API：通过 REST API 动态管理配置
- 多种存储后端：本地文件、PostgreSQL、Git、S3 对象存储

详细文档请参考：
- [README.md](README.md) - 完整功能说明
- [README_CN.md](README_CN.md) - 中文完整文档
- [MANAGEMENT_API.md](MANAGEMENT_API.md) - 管理 API 文档

---

## 💡 小贴士

1. **配置文件修改后会自动生效**，无需重启服务
2. **建议配置 3-5 个 API Key**，实现更好的负载均衡效果
3. **可以混合使用**官方 API Key 和第三方中转服务
4. **每个 Key 可以配置独立的代理**，灵活应对网络环境

---

## 🎉 开始使用吧！

现在你已经掌握了基本使用方法，快去试试吧！

如有问题，欢迎查看完整文档或加入社区：
- QQ 群：188637136
- Telegram 群：https://t.me/CLIProxyAPI
